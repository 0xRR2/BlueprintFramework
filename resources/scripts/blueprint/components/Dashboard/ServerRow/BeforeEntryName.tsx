import React from 'react';
/* blueprint/import */

import { Server } from '@/api/server/getServer';

interface Props {
  server: Server;
}

export default ({ server }: Props) => {
  return (
    <>
      {/* blueprint/react */}
    </>
  );
};