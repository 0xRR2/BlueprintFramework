import React from 'react';
/* blueprint/import */

import { Server } from '@/api/server/getServer';

export default ({ server }: Server) => {
  return (
    <>
      {/* blueprint/react */}
    </>
  );
};